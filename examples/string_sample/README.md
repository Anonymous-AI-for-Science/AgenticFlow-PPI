Place small STRING sample files in this directory when running the artifact
without the full dataset. The loader is header-driven and can operate on reduced
samples that preserve the original column names.


