requests>=2.31
numpy>=1.24
pandas>=2.0
torch>=2.0
torch-geometric>=2.5
h5py>=3.10
pyyaml>=6.0



# optional: nicer progress bars in long-running benchmarks (a stdlib fallback is used if absent)
tqdm>=4.66
